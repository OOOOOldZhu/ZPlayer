<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.z.zmusicplayer">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <!-- <meta-data>tools:replace="android:value"</meta-data> -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon_kg"
        android:label="@string/app_name"
        android:roundIcon="@drawable/yy"
        android:supportsRtl="true"
        android:theme="@style/MyAppTheme">
        <activity android:name=".zhu_demo.Zhu_Activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.MainActivity" />
        <activity android:name=".activity.MusicPlayerActivity" />
        <!--<activity android:name=".zhu_demo.Zhu_Activity"></activity>-->
        <service android:name=".zhu_demo.MusicplayService"/>
        <service android:name=".service.MusicplayerService"/>
    </application>

</manifest>